name: Steam Build

on:
  push:
    tags:
      - 'oh-first/*'
      - 'oh-two/*'
      - 'oh-last/*'
    branches: ['dev', 'autobuild']
  pull_request:
    branches: ['dev', 'autobuild']

jobs:
  # STEP 1: Identify which game we are building
  setup:
    runs-on: ubuntu-latest
    outputs:
      game_env: ${{ steps.get_env.outputs.game_env }}
    steps:
      - id: get_env
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            # Extracts "oh-first" from "oh-first/v1.0"
            TAG_NAME=${{ github.ref_name }}
            echo "game_env=${TAG_NAME%%/*}" >> $GITHUB_OUTPUT
          else
            # Default environment for branch pushes (non-tags)
            echo "game_env=oh-first" >> $GITHUB_OUTPUT
          fi

  build-windows:
    name: Windows Build
    needs: setup
    runs-on: windows-latest
    # Loads variables (STEAM_APPID) from the identified game environment
    environment: ${{ needs.setup.outputs.game_env }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Checkout Private Steam SDK
        uses: actions/checkout@v5
        with:
          repository: stesproject/steam-sdk
          token: ${{ secrets.SDK_TOKEN }}
          path: steam_sdk

      - uses: msys2/setup-msys2@v2
        with:
          msystem: mingw64
          install: >-
            base-devel git ruby vim
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-meson
            mingw-w64-x86_64-autotools
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-SDL2
            mingw-w64-x86_64-pixman
            mingw-w64-x86_64-expat

      - uses: actions/cache@v4
        with:
          path: |
            windows/build-mingw64
            windows/downloads
          key: win-${{ hashFiles('windows/Makefile') }}

      - name: Build all dependencies
        shell: msys2 {0}
        run: |
          cd windows
          make

      - name: Build exe
        shell: msys2 {0}
        run: |
          source windows/vars.sh
          rm -rf build
          # Use vars.STEAM_APPID from the Environment
          meson setup build -Denable-https=false -Dsteamworks_path=$PWD/steam_sdk -Dsteam_appid=${{ vars.STEAM_APPID }}
          cd build
          ninja
          find . -maxdepth 1 -name "*.exe" -exec strip {} \;

      - name: Prepare archive
        shell: msys2 {0}
        run: |
          mkdir -p build/artifact
          cp -r build/* build/artifact/ 2>/dev/null || true
          
          cd build/artifact
          rm -rf build.ninja compile_commands.json meson-private meson-logs meson-info *.o *.a artifact
          
          cp ../../steam_sdk/redistributable_bin/win64/steam_api64.dll .
          cp ../../windows/build-mingw64/bin/x64-msvcrt-ruby310.dll .
          
          cp -r ../../windows/build-mingw64/lib/ruby/3.1.0 .
          mv ./3.1.0 ./stdlib
          
          cp /mingw64/bin/zlib1.dll .
          cp /mingw64/bin/SDL2.dll .
          cp /mingw64/bin/libpixman-1-0.dll .
          cp /mingw64/bin/libexpat-1.dll .
          
          cp -r ../../scripts .
          # Use vars.STEAM_APPID from the Environment
          echo ${{ vars.STEAM_APPID }} > steam_appid.txt

          echo "--- WINDOWS COMPLETE ARTIFACT LIST ---"
          ls -Rlh

      - uses: actions/upload-artifact@v4
        with:
          # Names the artifact after the game (e.g., mkxp-z.windows-oh-two)
          name: mkxp-z.windows-${{ needs.setup.outputs.game_env }}
          path: build/artifact

  build-linux-native:
    name: Linux Build
    needs: setup
    runs-on: ubuntu-22.04
    environment: ${{ needs.setup.outputs.game_env }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Checkout